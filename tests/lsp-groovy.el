;;; lsp-groovy.el --- Testing groovy LSP mode -*- lexical-binding: t; -*-

;; (setq lsp-print-io t)

;; (find-file (file-truename (concat repodir "/tests/fixtures/groovy/groovycsv/src/com/xlson/groovycsv/CsvParser.groovy")))

;; (find-file (file-truename (concat repodir "/tests/fixtures/groovy/Jenkinsfile")))

(run-test "groovy")
